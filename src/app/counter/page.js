import State from "../State";

export const metadata = {
    title: 'this is a Couter page',
    description: 'you can counter anything you want..'
}

export default function Counter(){
    return(
        <State />
    )
}