// import "server-only"

export const serverSideFunction = () => {
    console.log(
        `use multi libery
            use env stuff
            intereact with db
        
        `
    );

    return "server result";
}